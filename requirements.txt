# Full stack for `python rafel.py train` (Phases I+II) and serving (Phase III).
# Core data / ML
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
lightgbm>=4.0
xgboost>=2.0
pyarrow>=14.0          # parquet caching of engineered features
joblib>=1.3           # model + artifact serialization

# Serving (Phase III)
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
