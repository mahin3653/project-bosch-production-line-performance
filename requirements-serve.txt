# Serving-only deps (keeps the image small — no jupyter / matplotlib / seaborn).
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
lightgbm>=4.0
xgboost>=2.0
joblib>=1.3
